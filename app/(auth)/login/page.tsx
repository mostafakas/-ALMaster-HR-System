import { LoginForm } from "@/components/auth/login-form";

export const metadata = {
  title: "Login | AlMaster HR System",
  description: "Log into your account to access the AlMaster HR System",
};

export default function LoginPage() {
  return <LoginForm />;
}
