import { RegisterForm } from "@/components/auth/register-form";

export const metadata = {
  title: "Register | AlMaster CRM System",
  description: "Create your account to access the AlMaster CRM System",
};

export default function RegisterPage() {
  return <RegisterForm />;
}
