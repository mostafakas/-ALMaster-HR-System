import { PersonalTargetView } from "@/components/project-management/my-target/personal-target-view";

export const metadata = {
  title: "Personal Target — AlMaster PM",
  description:
    "Track your own monthly target — All Tasks / To do list, Board / List / Calendar views.",
};

export default function MyTargetPage() {
  return <PersonalTargetView />;
}
